# remax ide

## Contribute

```bash
# install
$ pnpm i

# dev
$ pnpm run dev

# test
$ pnpm run test

# publish
$ pnpm run changeset # should generate `changeset` for each PR and commit to git.

$ pnpm run release:alpha # publish to npm
$ pnpm run release:beta
$ pnpm run release
```
